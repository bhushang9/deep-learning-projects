# 🧠 Handwritten Digit Recognition (MNIST)

A simple deep learning project using **Keras + MNIST dataset** to classify handwritten digits (0–9).  
The project includes preprocessing, model training, evaluation, saving the model, plotting digits, and making predictions.

---

## 📌 Project Overview

This project trains a **Convolutional Neural Network (CNN)** on the MNIST dataset.  
After training, the model is able to recognize handwritten digits with high accuracy(99%) and predict digits from sample images.

---

## 🚀 Features
- Load and preprocess MNIST dataset  
- Normalize image pixel values  
- Build a CNN model using Keras  
- Train the model  
- Evaluate model accuracy  
- Plot any digit from the dataset  
- Predict digit from sample image  
- Test prediction on random digits  

---

## 🧩 Dataset

The MNIST dataset contains:

- 60,000 training images  
- 10,000 testing images  
- Gray-scale 28×28 handwritten digits  

---

## 🛠 Technologies Used
- Python  
- NumPy  
- Matplotlib  
- TensorFlow / Keras  

---

## ✔️ Conclusion

This project successfully builds and trains a CNN model that recognizes handwritten digits with high accuracy.
Includes digit plotting, prediction, and random digit testing.

---
